
// Import from radix toast
import { toast as radixToast } from "@/components/ui/toast";

// Re-export the toast function
export const toast = radixToast;

// Re-export the useToast hook
export { useToast } from "@/components/ui/toast";
